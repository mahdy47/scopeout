"""scopeout.web - web/API layer.

Provides a read-only HTTP API and dashboard that reuse the existing
scopeout.core engine. The CLI and the web layer share the same core logic;
this package only adds request/response adapters around it.
"""
